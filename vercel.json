{
  "cleanUrls": true,
  "trailingSlash": false,
  "buildCommand": "",
  "outputDirectory": ".",
  "framework": null,
  "ignoreCommand": "git log -1 --pretty=format:'%ae' | grep -q 'emergent' && exit 0 || exit 1"
}
